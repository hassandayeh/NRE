# Use package-lock.json to ensure deterministic installs
save-exact=true

# Always respect the lockfile
ci=true

# Set default registry
registry=https://registry.npmjs.org/

# Avoid peer dep auto-install surprises
legacy-peer-deps=false
